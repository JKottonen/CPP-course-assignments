#ifndef SHIP_H
#define SHIP_H

using namespace std;

// Base class
class Ship {

    public:
        enum Direction {
            up, right, down, left
        };
        int length;
        Direction direction;

    private:

};


#endif